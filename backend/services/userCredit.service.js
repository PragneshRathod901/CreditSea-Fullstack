const userCreditModel = require("../models/userCredit.model");
const xml2js = require("xml2js");

class UserCreditsService {
  uploadXmlData = async (data) => {
    try {
    } catch (error) {
      throw error;
    }
  };
}
